using System.Data;

namespace _2024_06_10___Lesson__Intro_to_Microsoft_.NET_
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            object obj = new object();

            byte a = 255;
            byte b = 5;

            short c = 3;

            int s = 4;
            */
            /*
            Console.WriteLine("Hello world");
            Console.Write("Hello\n");
            Console.Write("\tHello\n");
            Console.Write("\t\tHello");
            Console.Write("Hello");
            Console.Write("Hello\n");

            Console.Write("Enter number : ");
            string str = Console.ReadLine();

            int number = int.Parse(str);
            Console.WriteLine(str + "!!!!!");
            Console.WriteLine(str + 100);
            Console.WriteLine(number + 100);
            Console.WriteLine("You entered " + number + 100 + "!!!!!");
            Console.WriteLine($"You entered { number + 100 }!!!!!");
            */
            /*
            // Not nullable
            int a = 0;

            // int* ptr = nullptr;
            // int* ptr = new int(5);
            // int r = null; // error


            // Nullable
            // Nullable<int> r = null;
            int? r = null;
            */
            /*
            DateTime now = DateTime.Now;
            Console.WriteLine($"Custom date : {now.ToString("yyyy-MM-dd")}");
            Console.WriteLine($"Custom time : {now.ToString("HH-mm-ss")}");
            */
            /*
            string str = null;
            if (str != null)
                str.ToUpper();
            string str2 = str ?? "Empty"; // null conditional operator
            */
        }
    }
}
